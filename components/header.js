import React from 'react';
import s from './dashbourd.module.css';

const Header = () => {
    return (
        <div className={s.header}>
            <div className={s.headeTitle}>Dashbourd</div>
        </div>
    )
};

export default Header;